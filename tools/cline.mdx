---
title: "Cline"
description: "What gets synced for Cline."
---

**Tool ID:** `cline`
**Config directory:** `~/.cline/`

## Synced files

| File / Directory | Type | Description |
|---|---|---|
| `cline_mcp_settings.json` | JSON | MCP server configuration |

## Example

```bash
lazy push --tool cline
lazy pull --tool cline
```
